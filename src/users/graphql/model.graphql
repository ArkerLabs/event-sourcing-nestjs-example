type User {
    id: String!
    name: String!
    email: String!
    created: String!
}

type Query {
  user(id: String!): User
  users: [User]
}

type Mutation {
  createUser(
    name: String!
    email: String!
  ): String
}
